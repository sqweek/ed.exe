package cmd

import (
	"os"
	"regexp"
	"fmt"
	"bufio"
	"./buf"
)

func parse(str string) cmd.Addr, cmd.Op {
	addr, rest = parseAddr
}

func parseAddr(str string) cmd.Addr, string {
	if str.HasPrefix("/") {
	
	}
}